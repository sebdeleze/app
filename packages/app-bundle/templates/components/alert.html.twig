{% if message %}
    <div class="p-4 mb-4 text-sm text-{{ this.color }}-800 rounded-lg bg-{{ this.color }}-50 dark:bg-gray-800 dark:text-{{ this.color }}-400" role="alert">
        {{ message }}
    </div>
    <div data-controller="hello"></div>
{% endif %}
